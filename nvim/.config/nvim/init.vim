set nocompatible
filetype off
if filereadable(expand("~/.config/nvim/vundle.vim"))
    source ~/.config/nvim/vundle.vim
endif
filetype plugin indent on
" Use tabs for indent
set shiftwidth=0
set expandtab
set tabstop=4

colors deus
